{
  "name": "web-components",
  "version": "0.0.1",
  "description": "Testing web components",
  "scripts": {
    "build": "npm run build --workspaces"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "typescript": "^5.5.4",
    "esbuild": "^0.23.0",
    "tsx": "^4.17.0"
  }
}
